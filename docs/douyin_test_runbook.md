# 抖音自动采集测试 Runbook

## 目标

把“已经验证过的方法”固定下来，后续所有真机验证都按这份 runbook 走，不再靠临场摸索。

## 一、当前稳定的基线路径

已验证正确的前半段路径：

1. 在 `com.example.a11yframework` 主界面点击 `开始抖音采集`
2. 手动冷启动抖音
3. 自动进入顶部 `团购`
4. 自动进入团购搜索输入页
5. 自动输入 `郑州美莱`
6. 自动进入商家结果页
7. 自动点击 `郑州美莱医疗美容医院` 结果带，进入店铺首页

## 二、当前固定点位

- 首页顶部 `团购`：`1016,213`
- 团购页搜索入口：`383,373`
- 团购 tab 已选中页右上角 `搜索`：`1340,219`
- 搜索页输入框：`695,215`
- 搜索页提交按钮：`1331,215`
- 结果页店铺入口带：
  - 区域：`[263,485][1384,548]`
  - 说明：点击整条标题带，不点下方统计行，不点团购卡

## 三、团购首页的两种已确认样式

### 1. 旧样式：红框搜索条

- 特征：
  - 页面上方直接出现搜索条
  - 左侧有城市，如 `郑州`
  - 中间可能出现 `美莱团购`
  - 右侧有 `搜索`
- 处理：
  - 直接点击搜索条中间区域，进入搜索输入页

### 2. 新样式：团购 tab 已选中 + 右上角搜索按钮

- 特征：
  - 顶部 tab 区出现 `已选中，团购，按钮`
  - 顶部右上角有单独 `搜索` 按钮
  - 页面底部仍有抖音主底栏 `首页 / 朋友 / 消息 / 我`
- 已验证素材：
  - `D:\project\adb\artifacts\groupbuy_current_c0e68dd.xml`
  - `D:\project\adb\artifacts\groupbuy_current_c0e68dd.png`
- 处理：
  - 直接点击右上角 `搜索`，可进入专用搜索输入页
  - 验证素材：
    - `D:\project\adb\artifacts\after_search_button_c0e68dd.xml`
    - `D:\project\adb\artifacts\after_search_button_c0e68dd.png`

## 四、当前固定页面锚点

### 1. 进入店铺首页的判定

同时满足这 2 个区域才算真正进入店铺首页：

- 上方商家信息区
  - `郑州美莱医疗美容医院`
  - `关注`
  - `回头客1千+ / 无隐形消费 / 详情 / 在线咨询 / 电话` 等店铺信息
- 底部固定操作栏
  - `医疗美容`
  - `订单`
  - `预约有礼`
  - `在线咨询`

### 2. 团购列表结束的判定

以下条件同时满足时，才认为团购列表到底：

- 团购区域下方出现 `收起`
- 同一区域出现 `预约到店送好礼` 或 `预约到店专属礼`
- 当前页已经没有任何 `展开更多`

### 3. 错误区域判定

一旦出现这些推荐/兴趣模块，就说明已经滚偏，不能继续把它当成团购主列表：

- `你可能感兴趣`
- `你可能感兴趣的地点`
- `猜你喜欢`

## 五、每轮测试的执行顺序

1. 先看当前 APK 是否是最新云编译产物
2. 第 1 轮必须从抖音冷启动开始跑
3. 第 1 轮结束后，再强制冷启动抖音，完整跑第 2 轮
4. 每一轮都要单独保留点击轨迹，不能只留最终截图
5. 先看当前页面是否符合 runbook 的前置页面
6. 只在前置页面成立时才执行固定点位
7. 每次路径跑通后，先留一张截图和一份 XML
8. 如果路径跑偏，先记录“从哪一步开始偏”，不要继续在错误页面上补点位

## 六、每轮测试必须沉淀的内容

每次真机测试结束后，至少补 1 项到文档里：

- 一份完整的点击轨迹
- 一个新的稳定点位
- 一个新的页面识别锚点
- 一个新的错误轨迹分析
- 一份新的“正确页面”截图/XML 基准资产

## 七、点击轨迹要求

- 关键点击至少要记录：
  - 开始抖音采集
  - 首页 `团购`
  - 团购页搜索入口
  - 搜索页 `搜索`
  - 结果页开店点击
  - 后续所有 `展开更多`
- 新版宿主会把点击轨迹写到应用私有文件：
  - `/data/user/0/com.example.a11yframework/files/tap-trace-latest.txt`
- 每次双轮测试后，都要把这份轨迹和截图/XML 一起看，不只看最终停留页

## 八、结果页开店纪律

- 结果页只允许点击“店铺入口带”，不允许把下方团购卡、问答卡、医院文案、点评文案当店铺入口。
- 如果同一页出现多个 `郑州美莱医疗美容医院`，优先选：
  - 位于结果页上半屏
  - 父级是一整条横向入口带
  - 上方没有团购商品标题
- 明确排除：
  - 团购卡里的医院署名
  - `继续追问`
  - `已售 / 人逛过 / 券后 / 次卡` 这类商品上下文

## 九、取证纪律

- 截图、XML、窗口信息不要并行抓。
- 原因：
  - 抖音页面切换很快，并行取证会把两个时刻混在一起，造成“截图是抖音，XML 却是宿主 App”这种假象。
- 正确顺序：
  1. 先截图
  2. 再抓 XML
  3. 再抓窗口/日志
  4. 最后拉点击轨迹

## 十、当前最重要的纪律

- 不在错误页面上重放首页坐标
- 不在首屏未稳定时抢先下滑
- 不把店铺尾部页误判成离开店铺
- 所有新成功方法都必须写回文档
