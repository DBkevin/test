# A11y 数据抓取框架 - 项目文档

> **版本**: v1.0.0  
> **最后更新**: 2026-03-10  
> **状态**: ✅ 基础功能已完成，可正常使用

---

## 📋 目录

- [项目概述](#项目概述)
- [已完成功能](#已完成功能)
- [项目结构](#项目结构)
- [技术架构](#技术架构)
- [配置说明](#配置说明)
- [使用指南](#使用指南)
- [已知问题](#已知问题)
- [后续计划](#后续计划)

---

## 项目概述

基于 Android 无障碍服务的数据抓取框架，支持美团、抖音等 APP 的团购数据自动抓取。

### 核心特性

- ✅ **插件化架构** - 每个 APP 独立插件，互不干扰
- ✅ **自定义关键词** - 可在 APP 界面自由配置抓取关键词
- ✅ **本地存储** - SQLite 数据库，支持 JSON 导出
- ✅ **实时抓取** - 自动检测目标页面并抓取数据

### 适用场景

- 医美行业价格监控（如：黄金微针、水光针、热玛吉等）
- 团购数据采集
- 竞品分析
- 市场动向观测

---

## 已完成功能

### ✅ v1.0.0 (2026-03-10)

| 功能模块 | 状态 | 说明 |
|----------|------|------|
| **基础框架** | ✅ 完成 | 无障碍服务、插件管理、配置管理 |
| **美团插件** | ✅ 完成 | 支持美团 APP 团购数据抓取 |
| **抖音插件** | ✅ 完成 | 支持抖音 APP 团购数据抓取 |
| **关键词配置** | ✅ 完成 | 可在 APP 界面自定义抓取关键词 |
| **数据存储** | ✅ 完成 | SQLite 本地存储 |
| **数据导出** | ✅ 完成 | 支持导出为 JSON 文件 |
| **测试数据** | ✅ 完成 | 可生成模拟数据用于测试 |
| **UI 界面** | ✅ 完成 | 主界面、服务开关、数据统计 |

### 已验证功能

```
✅ 应用正常启动
✅ 无障碍服务正常开启
✅ 关键词配置保存成功
✅ 抖音页面自动识别
✅ 数据自动抓取（已测试抓取 2 条数据）
✅ 数据本地存储
✅ 数据导出功能
```

---

## 项目结构

```
android-a11y-framework/
├── app/
│   ├── src/main/
│   │   ├── java/com/example/a11yframework/
│   │   │   ├── core/                      # 核心框架
│   │   │   │   ├── FrameworkAccessibilityService.kt  # 无障碍服务
│   │   │   │   ├── IAccessibilityPlugin.kt           # 插件接口
│   │   │   │   ├── PluginManager.kt                  # 插件管理器
│   │   │   │   └── ScrapedData.kt                    # 数据结构
│   │   │   ├── data/                      # 数据层
│   │   │   │   └── DataStore.kt           # SQLite 存储
│   │   │   ├── config/                    # 配置层
│   │   │   │   └── ConfigManager.kt       # SharedPreferences 配置
│   │   │   ├── plugins/                   # 插件实现
│   │   │   │   ├── MeituanPlugin.kt       # 美团插件
│   │   │   │   └── DouyinPlugin.kt        # 抖音插件
│   │   │   ├── utils/                     # 工具类
│   │   │   │   └── NodeUtils.kt           # 无障碍节点工具
│   │   │   └── MainActivity.kt            # 主界面
│   │   ├── res/                           # 资源文件
│   │   │   ├── layout/
│   │   │   │   └── activity_main.xml      # 主界面布局
│   │   │   ├── values/
│   │   │   │   ├── strings.xml            # 字符串资源
│   │   │   │   ├── themes.xml             # 主题配置
│   │   │   │   └── colors.xml             # 颜色资源
│   │   │   └── xml/
│   │   │       └── accessibility_service_config.xml  # 无障碍配置
│   │   └── AndroidManifest.xml            # 应用清单
│   └── build.gradle.kts                   # 构建配置
├── build.gradle.kts                       # 项目构建配置
├── settings.gradle.kts                    # 项目设置
├── gradle.properties                      # Gradle 配置
├── gradlew                                # Gradle Wrapper
└── README.md                              # 项目说明
```

---

## 技术架构

### 架构图

```
┌─────────────────────────────────────────────────────┐
│                   MainActivity                       │
│  - 服务开关 | 关键词配置 | 数据统计 | 数据导出       │
└────────────────────┬────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────┐
│            FrameworkAccessibilityService            │
│  - 服务生命周期 | 事件监听 | 插件调度                │
└────────────┬─────────────────────┬──────────────────┘
             │                     │
┌────────────▼────────┐   ┌────────▼────────────────┐
│   PluginManager     │   │   ConfigManager         │
│  - 插件注册/查找     │   │  - 关键词配置           │
└────────────┬────────┘   │  - 插件配置             │
             │            └─────────────────────────┘
┌────────────▼───────────────────────────────────────┐
│              IAccessibilityPlugin                  │
│  - isTargetPage() | scrapeData() | processData()  │
└────────────┬──────────────────────┬────────────────┘
             │                      │
┌────────────▼────────┐   ┌─────────▼───────────────┐
│  MeituanPlugin      │   │  DouyinPlugin           │
│  - 美团团购抓取      │   │  - 抖音团购抓取          │
│  - 关键词：团购，优惠 │   │  - 关键词：可配置        │
└─────────────────────┘   └─────────────────────────┘
             │                      │
             └──────────┬───────────┘
                        │
             ┌──────────▼───────────┐
             │     DataStore        │
             │  - SQLite 存储        │
             │  - JSON 导出          │
             └──────────────────────┘
```

### 数据流程

```
1. 用户设置关键词
   ↓
2. 保存到 ConfigManager
   ↓
3. 插件初始化时加载关键词
   ↓
4. 无障碍服务监听窗口变化
   ↓
5. 匹配目标 APP → 激活对应插件
   ↓
6. 检测目标页面（包含关键词）
   ↓
7. 抓取页面数据
   ↓
8. 存储到 SQLite
   ↓
9. 用户可导出 JSON
```

---

## 配置说明

### 1. 关键词配置

**位置**: APP 主界面 → 关键词输入框

**格式**: 支持多种分隔符
```
# 逗号分隔
黄金微针，水光针，热玛吉

# 空格分隔
黄金微针 水光针 热玛吉

# 换行分隔
黄金微针
水光针
热玛吉
```

**默认关键词**: `团购，优惠，套餐，券，商家`

**生效范围**: 同时应用到美团插件和抖音插件

### 2. 无障碍服务配置

**位置**: 设置 → 更多设置 → 无障碍 → 已下载的服务

**服务名称**: `A11y 数据抓取服务`

**权限**: 
- 读取窗口内容
- 监听窗口变化
- 模拟手势（可选）

### 3. 数据库配置

**路径**: `/data/data/com.example.a11yframework/databases/a11y_scraped_data.db`

**表结构**:
```sql
CREATE TABLE scraped_data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp INTEGER NOT NULL,        -- 时间戳
    plugin_id TEXT NOT NULL,           -- 插件 ID (meituan/douyin)
    page_type TEXT NOT NULL,           -- 页面类型
    data_type TEXT NOT NULL,           -- 数据类型
    content TEXT,                      -- JSON 格式内容
    raw_text TEXT,                     -- 原始文本
    metadata TEXT,                     -- 元数据
    created_at INTEGER NOT NULL        -- 创建时间
)
```

---

## 使用指南

### 快速开始

#### 1. 安装应用

```powershell
# 连接手机
adb connect 192.168.1.38:端口号

# 安装应用
adb install app-debug.apk -r
```

#### 2. 开启服务

1. 打开 APP
2. 点击"启动服务"
3. 跳转到无障碍设置
4. 开启 `A11y 数据抓取服务`

#### 3. 配置关键词

1. 在关键词输入框输入：`黄金微针，水光针`
2. 点击"保存规则"
3. 看到提示 `✅ 规则已保存`

#### 4. 开始抓取

1. 打开抖音或美团 APP
2. 搜索关键词（如：黄金微针）
3. 浏览搜索结果页面
4. 上下滚动加载更多

#### 5. 查看数据

1. 回到 A11y 框架 APP
2. 查看"数据统计"区域
3. 点击"导出数据"查看完整数据

### 日志监控

```powershell
# 连接手机
adb connect 192.168.1.38:端口号

# 监控抓取日志
adb logcat | grep -E "A11yFramework|Scraped"

# 正常抓取日志示例
I A11yFramework: Window changed to: com.ss.android.ugc.aweme
I A11yFramework: Activating plugin: 抖音
D A11yFramework: Target page detected, scraping...
I A11yFramework: Scraped 1 records
```

---

## 已知问题

### ⚠️ 已识别问题

| 问题 | 影响 | 状态 | 备注 |
|------|------|------|------|
| **HyperOS 限制** | 重启后需重新开启服务 | 待优化 | 系统限制 |
| **价格字段为空** | 部分页面无法提取价格 | 待优化 | 页面结构差异 |
| **React Native 页面** | 无法读取内容 | 待研究 | 美团部分页面 |
| **数据去重** | 可能重复抓取 | 待优化 | 需添加去重逻辑 |

### 🔍 待研究问题

1. **动态内容加载** - 部分 APP 使用懒加载，需要等待
2. **反爬检测** - 大规模抓取可能触发风控
3. **多账号支持** - 目前单账号运行

---

## 后续计划

### 🎯 v1.1.0 (计划中)

- [ ] **数据去重** - 基于内容哈希去重
- [ ] **价格提取优化** - 支持多种价格格式
- [ ] **商家名称提取** - 提取商家信息
- [ ] **销量提取** - 提取已售数量
- [ ] **抓取频率控制** - 可配置冷却时间

### 🎯 v1.2.0 (规划中)

- [ ] **多账号支持** - 支持切换账号
- [ ] **定时任务** - 定时自动抓取
- [ ] **数据同步** - 自动上传到服务器
- [ ] **更多 APP** - 支持小红书、大众点评等

### 🎯 v2.0.0 (长期规划)

- [ ] **云端控制** - Web 界面配置和查看
- [ ] **多设备管理** - 同时控制多台手机
- [ ] **数据分析** - 内置数据分析功能
- [ ] **告警通知** - 价格变动通知

---

## 开发记录

### 2026-03-10

**编译次数**: 33 次

**主要修复**:
1. ✅ 修复 AppCompat 主题不兼容问题
2. ✅ 修复 by lazy 与方法签名冲突
3. ✅ 修复类型转换安全问题
4. ✅ 添加自定义关键词配置功能
5. ✅ 修复 Gradle Wrapper 缺失
6. ✅ 添加 mipmap 图标资源

**测试验证**:
- ✅ 应用正常启动
- ✅ 关键词配置保存成功
- ✅ 抖音页面识别成功
- ✅ 数据抓取成功（2 条）
- ✅ 数据导出功能正常

---

## 相关资源

### 文档

- [DEBUG_GUIDE.md](DEBUG_GUIDE.md) - 调试指南
- [DEPLOY.md](DEPLOY.md) - 部署指南
- [README.md](README.md) - 项目说明

### 链接

- GitHub 仓库：https://github.com/DBkevin/test
- Android 无障碍服务文档：https://developer.android.com/guide/topics/ui/accessibility/service

---

## 联系方式

**开发者**: 李二 (Li Er)  
**Emoji**: 🦞

---

*最后更新：2026-03-10 18:35*
