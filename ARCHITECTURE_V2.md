# A11y 智能辅助 - 架构演进规划

> **版本**: v2.0 规划  
> **创建时间**: 2026-03-10  
> **状态**: 规划中  
> **目标**: 从静态插件架构演进为规则驱动的热加载架构

---

## 📋 目录

- [当前架构分析](#当前架构分析)
- [目标架构设计](#目标架构设计)
- [分阶段实现计划](#分阶段实现计划)
- [开发效率优化](#开发效率优化)
- [测试策略](#测试策略)
- [风险评估](#风险评估)

---

## 当前架构分析

### 现状

```
┌─────────────────────────────┐
│      智能辅助 (APP)          │
│  ┌─────────────────────┐   │
│  │  DouyinPlugin.kt    │   │  ← Kotlin 硬编码
│  │  MeituanPlugin.kt   │   │  ← Kotlin 硬编码
│  └─────────────────────┘   │
└─────────────────────────────┘
       ↑
       │ 修改需要
       │
  重新编译 → 推送 → 真机测试
  (5-10 分钟)
```

### 痛点

| 问题 | 影响 | 频率 |
|------|------|------|
| **每次修改要编译** | 浪费 5-10 分钟/次 | 高频 |
| **规则写死在代码里** | 调整关键词要改代码 | 高频 |
| **真机调试依赖** | 手机不在身边无法开发 | 中频 |
| **页面结构变化** | 需要重新分析 + 编译 | 中频 |
| **支持新 APP** | 需要写新插件 + 编译 | 低频 |

### 根本问题

**静态架构 vs 动态需求**

- 页面结构是**动态**的（APP 经常更新）
- 抓取规则是**动态**的（不同医院页面不同）
- 但我们的代码是**静态**的（编译后不可变）

---

## 目标架构设计

### 核心理念

> **规则驱动 + 脚本驱动 = 热加载能力**

### 架构图

```
┌─────────────────────────────────────────────────────┐
│              云端规则管理平台                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │ 规则配置     │  │ 脚本管理     │  │ 版本管理     │ │
│  │ (JSON)       │  │ (JavaScript)│  │ (Git)       │ │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘ │
└─────────┼────────────────┼────────────────┼────────┘
          │                │                │
          │ HTTP           │                │
          ▼                │                │
┌─────────────────────────────────────────────────────┐
│           智能辅助 (手机端)                          │
│  ┌─────────────────────────────────────────────┐   │
│  │            规则引擎 (Rule Engine)            │   │
│  │  - 解析 JSON 规则                             │   │
│  │  - 页面匹配 (Matcher)                        │   │
│  │  - 数据提取 (Extractor)                      │   │
│  └───────────────────┬─────────────────────────┘   │
│                      │                              │
│  ┌───────────────────▼─────────────────────────┐   │
│  │            脚本引擎 (Script Engine)          │   │
│  │  - JavaScript 解释器                          │   │
│  │  - 无障碍 API 封装                            │   │
│  │  - 沙箱隔离                                   │   │
│  └───────────────────┬─────────────────────────┘   │
│                      │                              │
│  ┌───────────────────▼─────────────────────────┐   │
│  │            执行引擎 (Executor)               │   │
│  │  - 点击/输入/滚动                            │   │
│  │  - 等待/重试                                 │   │
│  │  - 异常处理                                  │   │
│  └───────────────────┬─────────────────────────┘   │
│                      │                              │
│  ┌───────────────────▼─────────────────────────┐   │
│  │         Android 无障碍服务                    │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 规则格式示例

```json
{
  "rule_id": "douyin_hospital_v1",
  "app_id": "douyin",
  "app_package": "com.ss.android.ugc.aweme",
  "min_app_version": "20.0.0",
  "pages": [
    {
      "page_id": "hospital_detail",
      "match_rules": [
        {
          "type": "text_contains",
          "field": "page_text",
          "values": ["医院", "门诊", "整形"],
          "logic": "OR"
        }
      ],
      "extract_rules": {
        "hospital_name": {
          "type": "find_by_keywords",
          "keywords": ["医院", "门诊", "整形"],
          "location": "top",
          "max_depth": 3
        },
        "honors": {
          "type": "find_by_keywords",
          "keywords": ["认证", "奖", "基地", "定点"],
          "location": "near_hospital_name",
          "max_depth": 5
        },
        "group_buys": {
          "type": "find_list",
          "container": {
            "class": "androidx.recyclerview.widget.RecyclerView"
          },
          "item_rules": {
            "title": {
              "type": "find_by_keywords",
              "keywords": ["团购", "套餐", "体验", "次卡"]
            },
            "price": {
              "type": "regex",
              "pattern": "¥(\\d+(?:\\.\\d+)?)"
            },
            "sales": {
              "type": "find_by_keywords",
              "keywords": ["已售", "销量", "购买"]
            }
          }
        }
      }
    }
  ]
}
```

### 脚本格式示例

```javascript
// douyin_search_v1.js
(function() {
  // 配置
  const config = {
    appPackage: 'com.ss.android.ugc.aweme',
    timeout: 30000
  };
  
  // 主函数
  async function main(hospitalName) {
    log('开始执行：' + hospitalName);
    
    // 1. 打开抖音
    openApp(config.appPackage);
    wait(2000);
    
    // 2. 定位搜索框
    const searchBox = findOne('search', '搜索框');
    if (!searchBox) {
      error('未找到搜索框');
      return false;
    }
    
    // 3. 清空并输入
    clearText(searchBox);
    inputText(searchBox, hospitalName);
    
    // 4. 点击搜索
    click('搜索按钮');
    wait(3000);
    
    // 5. 抓取数据
    const data = extractData();
    
    // 6. 上报
    report(data);
    
    return true;
  }
  
  // 导出
  return { main };
})();
```

---

## 分阶段实现计划

### 阶段 1: 基础准备 (已完成 ✅)

**时间**: 2026-03-10  
**目标**: 完成基础功能，验证可行性

**已完成**:
- ✅ 基础无障碍服务框架
- ✅ 抖音/美团插件（硬编码版本）
- ✅ 数据提取逻辑
- ✅ 远程指令接收
- ✅ 搜索框操作

**交付物**:
- 可运行的 APK
- 基础测试数据

---

### 阶段 2: 规则引擎设计 (1-2 周)

**时间**: 2026-03-11 ~ 2026-03-24  
**目标**: 实现 JSON 规则热加载

#### 任务分解

| 任务 | 负责人 | 工时 | 优先级 |
|------|--------|------|--------|
| **规则格式定义** | 架构组 | 2 天 | P0 |
| **规则解析器** | 后端组 | 3 天 | P0 |
| **页面匹配引擎** | 架构组 | 3 天 | P0 |
| **数据提取引擎** | 架构组 | 3 天 | P0 |
| **规则下发接口** | 后端组 | 2 天 | P1 |
| **规则管理后台** | 前端组 | 5 天 | P2 |

#### 里程碑

```
Week 1:
  - 规则格式定义完成
  - 规则解析器完成
  - 页面匹配引擎完成

Week 2:
  - 数据提取引擎完成
  - 规则下发接口完成
  - 规则管理后台完成
  - 集成测试
```

#### 测试策略

**本地模拟测试** (无需真机):
```
┌─────────────────┐
│  规则管理后台    │
│  (Web 界面)      │
└────────┬────────┘
         │
         │ HTTP
         ▼
┌─────────────────┐
│  规则引擎模拟器  │
│  (Node.js)      │  ← 本地运行
└────────┬────────┘
         │
         │ 加载规则
         ▼
┌─────────────────┐
│  页面结构快照    │
│  (XML 文件)      │  ← 从真机导出一次
└─────────────────┘
```

**测试流程**:
1. 从真机导出页面 XML（只需一次）
2. 在本地模拟器加载规则
3. 验证规则匹配和提取
4. 调整规则（秒级反馈）
5. 规则验证通过后，推送到云端
6. 真机拉取规则测试

**效率提升**:
- 规则调整：秒级反馈（vs 5-10 分钟）
- 真机测试：只在最终验证时使用

---

### 阶段 3: 脚本引擎集成 (2-4 周)

**时间**: 2026-03-25 ~ 2026-04-14  
**目标**: 支持 JavaScript 脚本热加载

#### 技术选型

| 选项 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **Rhino** | Mozilla 出品，成熟 | 性能一般 | ⭐⭐⭐ |
| **QuickJS** | 轻量，性能好 | 生态较小 | ⭐⭐⭐⭐ |
| **Hermes** | React Native 同款 | 集成复杂 | ⭐⭐ |

**推荐**: QuickJS（轻量 + 性能好）

#### 任务分解

| 任务 | 负责人 | 工时 | 优先级 |
|------|--------|------|--------|
| **JS 引擎集成** | 架构组 | 3 天 | P0 |
| **无障碍 API 封装** | 架构组 | 5 天 | P0 |
| **沙箱隔离** | 架构组 | 3 天 | P0 |
| **脚本下发接口** | 后端组 | 2 天 | P1 |
| **脚本管理后台** | 前端组 | 5 天 | P2 |
| **脚本调试工具** | 架构组 | 5 天 | P2 |

#### 无障碍 API 封装示例

```javascript
// 暴露给脚本的 API
const A11y = {
  // 应用操作
  openApp(packageName),
  closeApp(packageName),
  
  // 查找节点
  findOne(keywords),
  findAll(keywords),
  findByClass(className),
  findByText(text),
  
  // 节点操作
  click(node),
  longClick(node),
  inputText(node, text),
  clearText(node),
  scroll(node, direction),
  
  // 等待
  wait(ms),
  waitFor(condition, timeout),
  
  // 日志
  log(message),
  error(message),
  
  // 数据上报
  report(data)
};
```

---

### 阶段 4: 完整插件系统 (长期)

**时间**: 2026-04-15 ~  
**目标**: 支持完整 APK 插件热加载

**关键问题**:
- 插件签名验证
- 插件权限控制
- 插件版本兼容
- 插件市场运营

**暂不紧急**, 等阶段 2、3 完成后再评估。

---

## 开发效率优化

### 问题

**当前流程**:
```
修改代码 → 编译 (3-5 分钟) → 推送真机 (1-2 分钟) 
→ 测试 → 发现问题 → 重复上述流程
```

**单次迭代**: 5-10 分钟  
**每天迭代**: 假设 10 次 = 50-100 分钟

### 解决方案

#### 1. 规则开发：本地模拟器 ✅

**工具**: `rule-simulator` (Node.js)

**流程**:
```bash
# 1. 导出页面结构（只需一次）
adb shell uiautomator dump /sdcard/window_dump.xml
adb pull /sdcard/window_dump.xml ./pages/hospital_detail.xml

# 2. 编写规则
cat rules/douyin_hospital.json

# 3. 本地测试
npx rule-simulator --rule rules/douyin_hospital.json --page pages/hospital_detail.xml

# 4. 查看结果
{
  "matched": true,
  "extracted": {
    "hospital_name": "北京 XX 医院",
    "honors": "XX 认证",
    "group_buys": [...]
  }
}
```

**效率**: 秒级反馈

#### 2. 脚本开发：Web 调试器

**工具**: `script-debugger` (Web 应用)

**界面**:
```
┌─────────────────────────────────────────┐
│  脚本调试器                              │
├─────────────────┬───────────────────────┤
│  脚本编辑器      │  控制台               │
│  ┌───────────┐  │  > log('test')        │
│  | function  |  │  test                 │
│  | main() {  |  │  > findOne('医院')    │
│  |   ...     |  │  {text: "XX 医院",...}│
│  | }         |  │                       │
│  └───────────┘  │                       │
├─────────────────┴───────────────────────┤
│  页面结构树 (可点击测试)                   │
│  └─ RecyclerView                        │
│     └─ LinearLayout                     │
│        ├─ TextView: "XX 医院" ✓         │
│        └─ TextView: "¥999" ✓            │
└─────────────────────────────────────────┘
```

**功能**:
- 脚本编辑 + 语法高亮
- 实时执行结果
- 页面结构可视化
- 节点点击测试

#### 3. 真机测试：按需使用

**原则**:
- 规则/脚本开发：本地模拟器（90% 时间）
- 集成测试：真机（10% 时间）

**真机测试场景**:
- 新规则首次验证
- 脚本功能完整测试
- 性能测试
- 稳定性测试

---

## 测试策略

### 测试金字塔

```
          /\
         /  \
        / E2E \      ← 真机测试 (10%)
       /────────\
      /   集成    \    ← 模拟器测试 (30%)
     /──────────────\
    /    单元测试     \  ← 本地测试 (60%)
   /──────────────────\
```

### 单元测试 (60%)

**测试对象**:
- 规则解析器
- 页面匹配引擎
- 数据提取引擎
- 脚本引擎 API

**工具**: Jest (Node.js)

**示例**:
```javascript
// rule-parser.test.js
describe('RuleParser', () => {
  test('should parse hospital rule', () => {
    const rule = parseRule(douyinHospitalRule);
    expect(rule.pages.length).toBe(1);
    expect(rule.pages[0].match_rules.length).toBe(1);
  });
  
  test('should match hospital page', () => {
    const matcher = new RuleMatcher(rule);
    const result = matcher.match(hospitalPageSnapshot);
    expect(result.matched).toBe(true);
  });
  
  test('should extract data', () => {
    const extractor = new DataExtractor(rule);
    const data = extractor.extract(hospitalPageSnapshot);
    expect(data.hospital_name).toBe('北京 XX 医院');
    expect(data.group_buys.length).toBe(4);
  });
});
```

**运行**:
```bash
npm test  # 秒级完成
```

### 集成测试 (30%)

**测试对象**:
- 规则引擎 + 脚本引擎
- 远程指令接收
- 数据上报

**工具**: 本地模拟器 + 页面快照

**示例**:
```bash
# 运行集成测试
npx rule-simulator --rule rules/douyin_hospital.json --page pages/hospital_detail.xml --verify

# 输出
✓ 规则解析成功
✓ 页面匹配成功
✓ 提取医院名称：北京 XX 医院
✓ 提取荣誉项：XX 认证
✓ 提取团单 4 个
✓ 数据格式验证通过
```

### E2E 测试 (10%)

**测试对象**:
- 完整流程（真机）
- 性能测试
- 稳定性测试

**工具**: ADB + 自动化脚本

**示例**:
```bash
# E2E 测试脚本
./e2e-test.sh

# 流程
1. 推送 APK 到真机
2. 安装应用
3. 配置规则
4. 打开抖音
5. 验证抓取结果
6. 生成测试报告
```

**频率**: 每天 1-2 次（非每次开发都跑）

---

## 风险评估

### 技术风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| **规则引擎性能** | 中 | 高 | 提前性能测试，优化算法 |
| **脚本引擎兼容性** | 中 | 中 | 选择成熟引擎（QuickJS） |
| **沙箱逃逸** | 低 | 高 | 严格权限控制，代码审查 |
| **规则格式变化** | 高 | 中 | 版本管理，向后兼容 |

### 进度风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| **规则格式设计延期** | 中 | 中 | 先 MVP，再迭代 |
| **模拟器开发延期** | 中 | 中 | 优先核心功能 |
| **真机测试问题** | 高 | 高 | 提前预留缓冲时间 |

### 应对策略

1. **MVP 优先**: 先实现核心功能（规则解析 + 匹配 + 提取）
2. **快速迭代**: 每周一个可测试版本
3. **充分测试**: 单元测试覆盖率 > 80%
4. **文档同步**: 每阶段更新文档

---

## 下一步行动

### 本周 (2026-03-11 ~ 2026-03-17)

- [ ] 规则格式定义完成
- [ ] 规则解析器完成
- [ ] 页面匹配引擎完成
- [ ] 本地模拟器 MVP 完成

### 下周 (2026-03-18 ~ 2026-03-24)

- [ ] 数据提取引擎完成
- [ ] 规则下发接口完成
- [ ] 规则管理后台完成
- [ ] 集成测试完成

### 下下周 (2026-03-25 ~)

- [ ] JS 引擎选型
- [ ] JS 引擎集成
- [ ] 无障碍 API 封装

---

## 总结

### 核心目标

> **让规则代替代码，让配置代替编译**

### 关键收益

| 指标 | 当前 | 目标 | 提升 |
|------|------|------|------|
| **规则调整时间** | 5-10 分钟 | 秒级 | 100x |
| **真机测试频率** | 每次 | 按需 | 10x 减少 |
| **支持新 APP** | 写代码 + 编译 | 配置规则 | 10x |
| **非技术人员参与** | 不可能 | 可以配置规则 | ∞ |

### 架构演进

```
v1.0 (当前): 静态插件架构
  ↓
v2.0 (阶段 2): 规则驱动架构
  ↓
v3.0 (阶段 3): 脚本驱动架构
  ↓
v4.0 (阶段 4): 完整插件生态
```

---

*最后更新：2026-03-10*  
*作者：李二 (Li Er) 🦞*
